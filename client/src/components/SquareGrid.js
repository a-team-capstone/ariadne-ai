import React, { PureComponent } from 'react'
import { Group } from 'react-art'
import Tile from './Tile'
import _ from 'lodash'

class SquareGrid extends PureComponent {

  findCoords (centerVector, size, axialX, axialY) {
    return {
      x: ,
      y: 
    }
  }

  render() {
    const widthPixels = this.props.width,
    heightPixels = this.props.height,
    hexCountHorizontal = this.props.hexCountHorizontal,
    hexCountVertical = this.props.hexCountVertical

    // const positions = 

    return (

    )
  }
}
